# Claude Code notes — Southern Pallet Recycling website

See `AGENTS.md` for the general agent-facing project context (stack, conventions, form/webhook pattern, what not to hardcode). This file is just the Claude-Code-specific pointer to it — there's nothing Claude-specific to add beyond what's already in `AGENTS.md` and `README.md`.

Quick orientation if you're picking this up cold:

- `README.md` — what this is, how to deploy, environment variables, forms/lead-capture, analytics, the business-plan page's security caveat.
- `AGENTS.md` — the same context framed for an AI agent making code changes: what patterns to keep, what not to hardcode.
- `env.example.txt` — the full list of optional environment variables with inline comments.

Read those three before making changes; there's no deeper internal documentation than what's there.
