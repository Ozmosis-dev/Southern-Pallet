# Southern Pallet — environment configuration
#
# Copy this file to .env.local (local dev) or set these in your Vercel
# project's Environment Variables (production). All are optional — the
# site runs fine with none of them set, just without lead delivery,
# tag manager, or a real business-plan gate.

# Resend API key used to email both lead forms. Create this through the Vercel
# Resend integration or the Resend dashboard. Keep it server-side and secret.
RESEND_API_KEY=

# Inbox that receives lead notifications (for example, info@example.com).
LEAD_NOTIFICATION_EMAIL=

# Verified Resend sender identity, including an optional display name
# (for example, Southern Pallet Website <leads@example.com>).
LEAD_FROM_EMAIL=

# Optional secondary delivery channel. Point this at a Zapier "Catch Hook",
# Make webhook, or any endpoint that accepts a JSON POST.
LEAD_WEBHOOK_URL=

# Optional outbound request timeout in milliseconds. Defaults to 8000.
LEAD_DELIVERY_TIMEOUT_MS=8000

# At least one complete delivery channel is required. Configure the three
# Resend variables above, LEAD_WEBHOOK_URL, or both.

# Google Tag Manager container ID (e.g. GTM-XXXXXXX). Leave unset and no
# GTM script loads at all.
NEXT_PUBLIC_GTM_ID=

# Password for the /private/business-plan page. This is a client-side-only
# check (see the code comment on that page) — fine for keeping the page out
# of casual view, not real access control. Defaults to "changeme" if unset.
NEXT_PUBLIC_BUSINESS_PLAN_PASSWORD=
